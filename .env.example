# Where the Code Intelligence console is deployed. Every "Open console" link on
# the page points here.
#
# Read at BUILD time, not at runtime - Vite inlines it into the bundle. Changing
# it means rebuilding, which is why the deploy steps in README.md set it before
# `npm run build` rather than after.
VITE_CONSOLE_URL=http://localhost:8765
